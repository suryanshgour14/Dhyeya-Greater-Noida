# Deploy Marker

This file forces Vercel to build the latest commit (real file change, not an
empty/merge commit which Vercel may skip).

Last deploy trigger: 2026-07 payment-flow go-live (reconcile + order-status routes).
